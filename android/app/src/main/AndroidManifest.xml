<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Deep Links: Ein Link auf fightbase.io oeffnet die App statt den
                Browser. autoVerify laesst Android beim Installieren
                https://fightbase.io/.well-known/assetlinks.json abrufen und den
                Fingerabdruck der Signatur vergleichen; erst wenn das passt,
                uebernimmt die App die Links ohne Rueckfrage.

                Die Ziel-URL laedt Capacitor nicht von selbst — das macht
                MainActivity.openDeepLink(), und zwar nur fuer die beiden Hosts
                unten.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!--
                    Ein Attribut je data-Tag. Android fuehrt die Attribute aller
                    data-Tags eines Filters zusammen und bildet daraus das
                    Kreuzprodukt — https x {fightbase.io, www.fightbase.io}.
                    Scheme und Host im selben Tag zu mischen ergibt dasselbe
                    Ergebnis, liest sich aber so, als waeren es zwei getrennte
                    Paare; Android Lint merkt das als
                    IntentFilterUniqueDataAttributes an.
                -->
                <data android:scheme="https" />
                <data android:host="fightbase.io" />
                <data android:host="www.fightbase.io" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
